{
  "name": "signet-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "SIGNET - Salary-backed confidential lending",
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {},
  "overrides": {
    "qr": "0.5.5"
  }
}
